{
  "hub": {
    "name": "Hub",
    "description": "A Hub represents a unique business concept and contains the business keys and metadata.",
    "fields": [
      {
        "name": "schema_name",
        "label": "Schema Name",
        "type": "text",
        "required": false,
        "placeholder": "user_spaces.user_obause",
        "help": "Optional schema for the hub table"
      },
      {
        "name": "table_name",
        "label": "Table Name",
        "type": "text",
        "required": true,
        "placeholder": "customer_h",
        "help": "Name of the Hub table (e.g., customer_h)"
      },
      {
        "name": "hub_hashkey_name",
        "label": "Hub Hashkey Column (target)",
        "type": "text",
        "required": true,
        "placeholder": "hk_customer_h",
        "help": "Target hashkey column name in the hub table"
      },
      {
        "name": "hub_business_key_name",
        "label": "Hub Business Key Column (target)",
        "type": "text",
        "required": true,
        "placeholder": "c_custkey",
        "help": "Target business key column name in the hub table"
      },
      {
        "name": "sources",
        "label": "Sources",
        "type": "hub-sources-config",
        "required": true,
        "placeholder": "",
        "help": "Add one or more source stage views/tables and map their hashkey, business key, ldts and rsrc columns"
      }
    ],
    "template": "file:hub.sql",
    "incrementalTemplate": "file:hub_incremental.sql"
  },
  "satellite": {
    "name": "Satellite",
    "description": "A Satellite contains descriptive attributes and tracks changes over time for a Hub or Link.",
    "fields": [
      { "name": "schema_name", "label": "Schema Name", "type": "text", "required": false, "placeholder": "user_spaces.user_obause", "help": "Optional schema for the satellite table" },
      { "name": "table_name", "label": "Table Name", "type": "text", "required": true, "placeholder": "customer_p0_s", "help": "Name of the Satellite table (e.g., customer_p0_s)" },
      { "name": "parent_hashkey_name", "label": "Parent Hashkey Column (in source)", "type": "text", "required": true, "placeholder": "hk_customer_h", "help": "Parent hashkey column provided by stage view" },
      { "name": "hashdiff_name", "label": "Hashdiff Column (in source)", "type": "text", "required": true, "placeholder": "hd_customer_p_s", "help": "Hashdiff column provided by stage view for this satellite split" },
      { "name": "descriptive_columns", "label": "Descriptive Columns", "type": "multi-text", "required": true, "placeholder": "C_NAME", "help": "Descriptive attributes captured in this satellite" },
      { "name": "source_table", "label": "Source Table", "type": "text", "required": true, "placeholder": "user_spaces.user_obause.stg_customer", "help": "Source stage view/table" },
      { "name": "ldts_column", "label": "LDTS Column", "type": "text", "required": true, "placeholder": "LDTS", "help": "Load datetime column name in source (quoted in SQL)" },
      { "name": "rsrc_column", "label": "RSRC Column", "type": "text", "required": true, "placeholder": "RSRC", "help": "Record source column name in source (quoted in SQL)" }
    ],
    "template": "file:satellite.sql",
    "incrementalTemplate": "file:satellite_incremental.sql"
  },
  "multi_active_satellite": {
    "name": "Multi-Active Satellite",
    "description": "A Multi-Active Satellite that allows multiple active rows per business key, with an additional multi-active key.",
    "fields": [
      { "name": "schema_name", "label": "Schema Name", "type": "text", "required": false, "placeholder": "user_spaces.user_obause", "help": "Optional schema for the MAS table" },
      { "name": "table_name", "label": "Table Name", "type": "text", "required": true, "placeholder": "customer_ma_s", "help": "Name of the Multi-Active Satellite table" },
      { "name": "parent_hashkey_name", "label": "Parent Hashkey Column (in source)", "type": "text", "required": true, "placeholder": "hk_customer_h", "help": "Parent hashkey column provided by stage view" },
      { "name": "multi_active_key_name", "label": "Multi-Active Key (in source)", "type": "text", "required": true, "placeholder": "hd_customer_p_s", "help": "Column distinguishing multiple active rows for a key (e.g., a line id or composite hash)" },
      { "name": "descriptive_columns", "label": "Descriptive Columns", "type": "multi-text", "required": true, "placeholder": "C_COMMENT", "help": "Descriptive attributes captured in this MAS" },
      { "name": "source_table", "label": "Source Table", "type": "text", "required": true, "placeholder": "user_spaces.user_obause.stg_customer", "help": "Source stage view/table" },
      { "name": "ldts_column", "label": "LDTS Column", "type": "text", "required": true, "placeholder": "LDTS", "help": "Load datetime column name in source (quoted in SQL)" },
      { "name": "rsrc_column", "label": "RSRC Column", "type": "text", "required": true, "placeholder": "RSRC", "help": "Record source column name in source (quoted in SQL)" }
    ],
    "template": "file:multi_active_satellite.sql",
    "incrementalTemplate": "file:multi_active_satellite_incremental.sql"
  },
  "link": {
    "name": "Link",
    "description": "A Link represents relationships between business entities (Hubs).",
    "fields": [
      { "name": "schema_name", "label": "Schema Name", "type": "text", "required": false, "placeholder": "user_spaces.user_obause", "help": "Optional schema for the link table" },
      { "name": "table_name", "label": "Table Name", "type": "text", "required": true, "placeholder": "customer_nation_l", "help": "Name of the Link table (e.g., customer_nation_l)" },
      { "name": "link_hashkey_name", "label": "Link Hashkey Column (target)", "type": "text", "required": true, "placeholder": "hk_customer_nation_l", "help": "Target link hashkey column name" },
      { "name": "source_table", "label": "Source Table", "type": "text", "required": true, "placeholder": "user_spaces.user_obause.stg_customer", "help": "Source stage view/table" },
      { "name": "link_hashkey_source_column", "label": "Link Hashkey Source Column", "type": "text", "required": true, "placeholder": "hk_customer_nation_l", "help": "Column in source providing link hashkey" },
      { "name": "related_hubs", "label": "Related Hubs", "type": "link-hubs-config", "required": true, "placeholder": "", "help": "Add two or more related hubs: map the target hub hashkey column name in the link and its source column" },
      { "name": "ldts_column", "label": "LDTS Column", "type": "text", "required": true, "placeholder": "ldts", "help": "Load datetime column in source" },
      { "name": "rsrc_column", "label": "RSRC Column", "type": "text", "required": true, "placeholder": "rsrc", "help": "Record source column in source" },
      { "name": "is_transactional", "label": "Transactional Link", "type": "checkbox", "required": false, "help": "Include additional descriptive columns in the link" },
      { "name": "transactional_columns", "label": "Transactional Columns", "type": "multi-text", "required": false, "placeholder": "c_acctbal", "help": "Descriptive columns to include when Transactional Link is enabled" }
    ],
    "template": "file:link.sql",
    "incrementalTemplate": "file:link_incremental.sql"
  },
  "pit": {
    "name": "Point-in-Time (PIT)",
    "description": "A PIT table provides a snapshot view of data at specific points in time.",
    "fields": [
      { "name": "tracked_entity", "label": "Tracked Entity Type", "type": "text", "required": true, "placeholder": "hub or link", "help": "Whether you track a hub or a link" },
      { "name": "tracked_entity_hashkey", "label": "Tracked Entity Hashkey Name", "type": "text", "required": true, "placeholder": "hk_customer_h", "help": "Hashkey column name of the tracked entity in the source" },
      { "name": "satellites", "label": "Satellites", "type": "satellites-config", "required": true, "placeholder": "", "help": "List the satellites to join, with table and alias" },
      { "name": "snapshot_table", "label": "Snapshot Table Name", "type": "text", "required": true, "placeholder": "user_spaces.user_obause.snap_v1", "help": "Qualified snapshot table or view to join for activity windows" },
      { "name": "dimension_key_name", "label": "Dimension Key Name", "type": "text", "required": true, "placeholder": "hk_customer_d", "help": "Name of the PIT dimension key column to generate" },
      { "name": "table_name", "label": "Target PIT Table", "type": "text", "required": true, "placeholder": "user_spaces.user_obause.customer_bp", "help": "Fully qualified target PIT table" }
    ],
    "template": "file:pit.sql",
    "incrementalTemplate": "file:pit_incremental.sql"
  },
  "stage_view": {
    "name": "Stage View",
    "description": "A staging view that calculates hash keys, hash diffs, and Data Vault specific columns with ghost records.",
    "fields": [
      {
        "name": "view_name",
        "label": "View Name",
        "type": "text",
        "required": true,
        "placeholder": "stg_customer",
        "help": "Name of the staging view"
      },
      {
        "name": "schema_name",
        "label": "Schema Name",
        "type": "text",
        "required": false,
        "placeholder": "user_spaces.user_obause",
        "help": "Schema where the view will be created"
      },
      {
        "name": "source_table",
        "label": "Source Table",
        "type": "text",
        "required": true,
        "placeholder": "SNOWFLAKE_SAMPLE_DATA.TPCH_SF1.Customer",
        "help": "Full path to the source table"
      },
      {
        "name": "source_columns",
        "label": "Source Columns",
        "type": "column-config",
        "required": false,
        "placeholder": "",
        "help": "Define columns with their data types for correct ghost record values"
      },
      {
        "name": "auto_include_all_source_columns",
        "label": "Automatically include all columns from source table",
        "type": "checkbox",
        "required": false,
        "help": "Selects all columns from the source table automatically and skips per-column ghost records"
      },
      {
        "name": "hashkeys",
        "label": "Hash Keys Configuration",
        "type": "hashkey-config",
        "required": true,
        "placeholder": "",
        "help": "Define hash keys with their business key columns"
      },
      {
        "name": "hashdiffs",
        "label": "Hash Diffs Configuration", 
        "type": "hashdiff-config",
        "required": false,
        "placeholder": "",
        "help": "Define hash diffs for satellite tracking"
      },
      {
        "name": "use_current_timestamp",
        "label": "Use Current Timestamp for Load Date",
        "type": "checkbox",
        "required": false,
        "help": "Check to use SYSDATE(), uncheck to specify existing column"
      },
      {
        "name": "load_date_column",
        "label": "Load Date Column",
        "type": "text", 
        "required": false,
        "placeholder": "LOAD_DATETIME",
        "help": "Existing column name for load date (if not using current timestamp)"
      },
      {
        "name": "use_static_record_source",
        "label": "Use Static Record Source",
        "type": "checkbox",
        "required": false,
        "help": "Check to use static text, uncheck to specify existing column"
      },
      {
        "name": "record_source_value",
        "label": "Record Source Value",
        "type": "text",
        "required": false,
        "placeholder": "TPC_H_SF1.Customer",
        "help": "Static text or existing column name for record source"
      }
    ],
    "template": "file:stage_view.sql"
  }
  ,
  "snapshot": {
    "name": "Snapshot Table",
    "description": "A snapshot table for time-based snapshots. Supports multiple versions.",
    "fields": [],
    "template": "file:snapshot_v0.sql"
  }
} 